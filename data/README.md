# Data

This repository contains the full database schema and stored procedures in `sql/schema.sql`.
Sample seed data is included directly in the SQL file via `INSERT` statements — no external data files are required.

To set up the database, run `sql/schema.sql` against a SQL Server instance.
